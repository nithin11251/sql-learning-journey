-- INSERT
INSERT INTO emp
VALUES (9000, 'NITHIN', 'CLERK', 7902, SYSDATE, 2000, NULL, 20);

-- UPDATE
UPDATE emp
SET sal = 2500
WHERE empno = 9000;

-- DELETE
DELETE FROM emp
WHERE empno = 9000;

-- COMMIT
COMMIT;

-- ROLLBACK
ROLLBACK;
